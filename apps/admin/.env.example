NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. Never prefix with NEXT_PUBLIC_. Used exclusively by
# src/lib/supabase/admin-auth.ts for the five named Auth Admin API operations -- never for
# ordinary database reads, never sent to a Client Component, never logged.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
